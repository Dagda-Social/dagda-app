enum ContentType { post, video, image, podcast }

enum UserStatus { active, inactive, banned, deleted }

enum Role { user, moderator, admin }

enum PostStatus { active, inactive, deleted, banned, pending }

enum Language { en, es, gl }

enum PageContentType { about, contact, privacy, terms, faq, help }